# Windows checkouts must not rewrite these to CRLF. Every tracked file here is
# read by bash on Ubuntu or mounted into a Linux container, where a CR is a
# syntax error rather than whitespace — and the resulting message names neither
# the file nor the cause. See playbook-traefik-integration.md.
#
# This is committed rather than left to core.autocrlf because that setting is
# per clone: a deployment must not depend on how a laptop is configured.
* text=auto eol=lf

# Explicit for the files that actually break, so Git's text/binary heuristic is
# never in the loop for them.
*.sh text eol=lf

# The heuristic's real failure mode is calling a binary "text" and mangling it.
*.png binary
*.jpg binary
*.gz binary
*.zip binary
